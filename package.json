{
  "name": "monorepo-root",
  "private": true,
  "workspaces": [
    "Backend/",
    "Frontend/",
    "cli/",
    "lib/"
  ],
  "scripts": {
    "cli": "npm run start --workspace=cli",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "prettier": "^3.0.3"
  }
}
